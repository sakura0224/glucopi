// Public-safe API endpoint placeholders.
// Replace these with your actual deployment endpoints before running the app.

const HTTP_BASE_URL = 'https://your-api-domain.com/api/v1'
const WS_BASE_URL = 'wss://your-api-domain.com/api/v1/ws/chat'

module.exports = {
  HTTP_BASE_URL,
  WS_BASE_URL
}
